const CITY='249d9391-a604-4ade-8c0a-72ed98b6f261',A=window.ArapongasDigitalAPI,money=n=>n==null?'Consulte':new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(n));
function safeImage(value){try{const u=new URL(value,location.origin);return ['http:','https:'].includes(u.protocol)?u.href:''}catch{return''}}
function el(tag,text,cls){const n=document.createElement(tag);if(text!=null)n.textContent=text;if(cls)n.className=cls;return n}
function productCard(p){const card=el('article',null,'product'),photo=el('div',null,'photo blue'),imgUrl=safeImage(p.image_url);if(imgUrl){const img=document.createElement('img');img.src=imgUrl;img.alt=p.name||'Produto';img.loading='lazy';img.decoding='async';img.style='width:100%;height:100%;object-fit:cover';photo.append(img)}else photo.append(el('span','🛍️'));if(p.promotional_price!=null)photo.append(el('span','OFERTA'));const info=el('div',null,'info');info.append(el('small',p.category_name||'COMÉRCIO LOCAL'),el('h3',p.name||'Produto'),el('p',p.business_name||'Empresa local'));if(p.promotional_price!=null&&p.price!=null)info.append(el('del',money(p.price)));info.append(el('strong',money(p.promotional_price??p.price)));const a=el('a','Ver detalhes');a.href='pages/produto.html?id='+encodeURIComponent(p.id||'');info.append(a);card.append(photo,info);return card}
async function loadHome(){const grid=document.querySelector('#productGrid');if(!grid)return;try{const products=await A.featuredProducts(CITY);if(!products.length){const empty=el('div','Ainda não há produtos em destaque cadastrados. As novidades das empresas aparecerão aqui.');empty.style='grid-column:1/-1;text-align:center;padding:35px;color:#6d7d93';grid.replaceChildren(empty);return}grid.replaceChildren(...products.slice(0,8).map(productCard))}catch{const error=el('div','Não foi possível carregar os destaques agora.');error.style='grid-column:1/-1;text-align:center;padding:35px;color:#6d7d93';grid.replaceChildren(error)}}
function demoLocation(){if(navigator.geolocation)navigator.geolocation.getCurrentPosition(()=>location.href='pages/explorar.html',()=>alert('Você pode continuar navegando por Arapongas sem compartilhar sua localização.'))}
loadHome();if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{});