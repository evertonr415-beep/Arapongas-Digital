const CITY='249d9391-a604-4ade-8c0a-72ed98b6f261',A=window.ArapongasDigitalAPI,E=A.escape,money=n=>n==null?'Consulte':new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(n));
async function loadHome(){const grid=document.querySelector('#productGrid');if(!grid)return;try{const products=await A.featuredProducts(CITY);if(!products.length){grid.innerHTML='<div style="grid-column:1/-1;text-align:center;padding:35px;color:#6d7d93">Ainda não há produtos em destaque cadastrados. As novidades das empresas aparecerão aqui.</div>';return}grid.innerHTML=products.slice(0,8).map(p=>`<article class="product"><div class="photo blue">${p.image_url?`<img src="${E(p.image_url)}" alt="" style="width:100%;height:100%;object-fit:cover">`:'🛍️'}${p.promotional_price!=null?'<span>OFERTA</span>':''}</div><div class="info"><small>${E(p.category_name||'COMÉRCIO LOCAL')}</small><h3>${E(p.name)}</h3><p>${E(p.business_name||'Empresa local')}</p>${p.promotional_price!=null&&p.price!=null?`<del>${money(p.price)}</del>`:''}<strong>${money(p.promotional_price??p.price)}</strong><a href="pages/produto.html?id=${encodeURIComponent(p.id)}">Ver detalhes</a></div></article>`).join('')}catch(e){grid.innerHTML='<div style="grid-column:1/-1;text-align:center;padding:35px;color:#6d7d93">Não foi possível carregar os destaques agora.</div>'}}
function demoLocation(){if(navigator.geolocation)navigator.geolocation.getCurrentPosition(()=>location.href='pages/explorar.html',()=>alert('Você pode continuar navegando por Arapongas sem compartilhar sua localização.'))}
loadHome();if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{});